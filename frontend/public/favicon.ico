from PIL import Image
import io

# Load the original image
image_path = "/mnt/data/a9424286-08d3-487e-bbb3-2356bde87da0.png"
img = Image.open(image_path)

# Resize and convert to .ico format
ico_path = "/mnt/data/favicon.ico"
img.save(ico_path, format='ICO', sizes=[(32, 32), (48, 48), (64, 64)])

ico_path
